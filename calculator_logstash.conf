input {
  file {
    path => "/home/pratap/Desktop/CalculatorLogs/CalculatorDevOps.log"
    start_position => "beginning"
  }
}

filter {
  grok {
    match => [
      "message", "%{HTTPDATE:timestamp_string} \[%{GREEDYDATA:thread}\] \[%{LOGLEVEL:level}\] %{GREEDYDATA:logger} \[%{GREEDYDATA:action}\] \- %{GREEDYDATA:line}"
    ]
  }

  date {
    match => ["timestamp_string", "dd/MMM/YYYY:HH:mm:ss SSS"]
  }

  mutate {
    remove_field => [timestamp_string]
  }
}

output {
  elasticsearch {
    hosts => ["https://4f1451f7384f4462a444e4789210f518.us-central1.gcp.cloud.es.io:9243"]
    user => "elastic"
    password => "iI4ZjKX7hwkuZboCa8pWd7rX"
    index => "calculator_elastic"
    document_type => "mylogs"
  }
  stdout {}  
  
}
